1) gcloud init
2) gcloud auth application-default login
2) gcloud auth login
3) gcloud services enable containerregistry.googleapis.com
4) Create Default Service Account in IAM & Admin, Service Accounts, assign Kubernetes Engine Admin role
5) You will need this email in ecs.tf
6) gcloud container clusters get-credentials https://cloud.google.com/kubernetes-engine/docs/how-to/cluster-access-for-kubectl
6a) You may have to change your KUBECONFIG variable to /Users/<insert your username here>/.kube/config
6b) to get the exact credentials command, you can click on your cluster name in the Google Cloud console, then select the CONNECT console button